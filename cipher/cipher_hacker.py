from sans_program_project.cipher.cipher_engine import _caesar_encrypt, _text2bytes 


def cipher_hacker(plaintext, ciphertext):
    my_bytes = _text2bytes(ciphertext)
    plain_bytes = _text2bytes(plaintext)
    import pdb; pdb.set_trace()
    print("done")

    
